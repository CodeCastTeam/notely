<!DOCTYPE html>
<html>
  <head>
    <title>Notely</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <div class="container">
      <!-- <a href="/">Home</a> -->
      <%# the line below 👇 generates the line above 👆 %>
      <div class="row">
        <div class="col">
          <%= link_to("Home", root_path) %>
        </div>
        <div class="col text-right">
          <% if user_sign_in? %>
            Hello <%= current_user.first_name %>
            <%= link_to "Sign out", sign_out_path, method: :delete %>
          <% end %>
        </div>
      </div>
      

      <hr>
      <%= yield %>
    </div>
  </body>
</html>
